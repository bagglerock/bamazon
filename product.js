var Product = function(id) {

    this.id = id;
  
    this.showInventory = function(func, lowQuantity){ //might be better in the interface
    }
  
    this.changeQuantity = function(product, action, quantity){
    }
  
  };
  
  module.exports = Product;